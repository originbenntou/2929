version: '3'
services:
  graphql:
    build:
      context: ./
      # prd
      # dockerfile: graphql/Dockerfile
      # local
      dockerfile: graphql/docker-local/Dockerfile
    container_name: 2929graphql
    ports:
      - "8080:8080"
    # local
    volumes:
      - ./graphql:/go/src/github.com/originbenntou/2929BE/graphql
  account:
    build:
      context: ./
      # prd
      # dockerfile: account/Dockerfile
      # local
      dockerfile: account/docker-local/Dockerfile
    container_name: 2929account
    ports:
      - "50051:50051"
    # local
    volumes:
      - ./account:/go/src/github.com/originbenntou/2929BE/account
  mysql:
    build:
      context: ./
      dockerfile: mysql/Dockerfile
    container_name: 2929mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: account
      MYSQL_USER: 2929
      MYSQL_PASSWORD: 2929
    ports:
      - "3306:3306"
    volumes:
      - ./mysql/data:/var/lib/mysql
