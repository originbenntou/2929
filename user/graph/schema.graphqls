type Query {
  user(id: ID!): User
  text(textCondition: TextCondition!): Text
}

type Text {
  textId: ID!
  textData: String!
}

input TextCondition {
  textId: ID!
}

type User {
  id: ID!
  name: String!
  groups(left: Boolean! = false): [Group!]!
}

type Group {
  id: ID!
}
